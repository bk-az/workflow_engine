<!-- DEVISE ONLY PUTS DATA INTO FLASH DICT. USING KEYS 'notice' OR 'alert' -->
<% flash.each do |key, value| %>
  <% if key == 'notice' || key == 'alert' %>
    <div class="alert alert-danger">
      <a href="#" class="close" data-dismiss="alert">&times;</a>
      <%= value %>
    </div>
  <% end %>
<% end %>

