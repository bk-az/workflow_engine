<% unless @watcher.nil? %>
  $('#create_watcher_<%= @watcher.id %>').fadeOut('slow');
<% end %>
// If admin adds current_user as watcher
$("#watch_button").remove();
$("#watch_button_div").append('<%= j render partial: "watch_button", locals: {issue: @issue} %>');
