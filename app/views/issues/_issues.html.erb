<!-- Partial: Displays rows in a table -->
<% issues.each do |issue| %>
  <tr>
    <td><%= link_to issue.title, project_issue_path(issue.project, issue) %></td>
    <% if request.path.include?('issues') %>
      <td><%= link_to issue.project.title, project_path(issue.project.sequence_num) %></td>
    <% end %>
    <td><%= link_to issue.assignee.try(:name), member_path(issue.assignee) %></td>
    <td><%= issue.issue_type.name %></td>
    <td><%= issue.issue_state.name %></td>
    <td><%= get_priority(issue.priority) %></td>
    <td><%= issue.start_date %></td>
    <td><%= issue.due_date %></td>
  </tr>
<% end %>