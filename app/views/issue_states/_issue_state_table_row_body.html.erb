<td><%= issue_state.name %></td>
<td>
  <% if issue_state.issue_id.nil? %>
    <i class="fas fa-check-circle"></i>
  <% else %>
    <i class="fas fa-times-circle"></i>
  <% end %>
</td>
<td><%= issue_state_issue_number(issue_state) %></td>
<td> 
  <%= link_to('Delete', issue_state_path(issue_state), method: 'delete', remote: true, data: { confirm: 'Are you sure to delete this issue state?'}, class: 'btn btn-danger btn-sm text-white mr-2 mb-1 mb-md-0 fixed-width') %>
  <% if @issue.present? %>
    <%= link_to('Edit', edit_issue_issue_state_path(@issue, issue_state), remote: true, class: 'btn btn-primary btn-sm text-white mr-2 mb-1 mb-md-0 fixed-width') %>
    <%= link_to('Show', issue_issue_state_path(@issue, issue_state), remote: true, class: 'btn btn-success btn-sm text-white fixed-width') %>
  <% else %>
    <%= link_to('Edit', edit_issue_state_path(issue_state), remote: true, class: 'btn btn-primary btn-sm text-white mr-2 mb-1 mb-md-0 fixed-width') %>
    <%= link_to('Show', issue_state_path(issue_state), remote: true, class: 'btn btn-success btn-sm text-white fixed-width') %>
  <% end %>
</td>