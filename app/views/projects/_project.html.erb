<%= '<div class="row">'.html_safe if project_counter%3 == 0%>
  <div class="col-md-4">
    <div class="card my-2">
      <%= image_tag("default.jpg", class: "card-img-top img-thumbnail") %>
      <div class="card-body">
        <h5 class="card-title"><%= link_to project.title, project%>
          <span class="badge badge-danger badge-pill float-right"><%= link_to 'Issues', '#', class: 'text-white'%></span>
        </h5>
        <p class="card-text text-truncate"><%= project.description%></p>
      </div>
      <div class="card-footer">
        <small class="text-muted">Last updated <%= time_ago_in_words(project.updated_at) %> ago</small>
      </div>
    </div>
  </div>
<%= "</div>".html_safe if ((project_counter+1)%3 == 0) || (project_counter+1) == project_iteration.size%>
