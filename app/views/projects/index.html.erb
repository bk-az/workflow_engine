<h1>Projects</h1>
<% @projects.each do |project| %>
  <div class="well">
    <h3><%= project.title %></h3>
    <p><%= project.description %></p>
    <%= link_to "Read More", project_path(project), :class => 'btn btn-primary' %>
  </div>
<% end %>